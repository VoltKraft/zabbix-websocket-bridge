services:
  zabbix-websocket-bridge:
    build: .
    image: zabbix-websocket-bridge:local
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      LOG_LEVEL: info
    ports:
      - "8080:8080"
    restart: unless-stopped
